parameters:
    level: 5
    paths:
        - src/
    bootstrapFiles:
        - vendor/autoload.php
    ignoreErrors:
        # CakePHP functions loaded at runtime
        - '#Function env not found#'

        # PHPDoc type certainty - false positives
        - '#will always evaluate to true#'
        - '#always exists and is not nullable#'
